using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * Author: [Satcher, Will]
 * Date created: [12/8/2024]
 * Date edited: [12/8/2024]
 * [Handles teleportation between levels]
 */


public class PortalUI : MonoBehaviour
{

    //public GameObject spawnPoint;

    //make an onclick so that when level is done, player can click to move to next level
    //make sure score is kept between 4 levels (should stay from teleporting)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
